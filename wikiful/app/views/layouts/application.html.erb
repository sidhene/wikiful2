<!DOCTYPE html>
<html>
<head>
  <title>Wikiful</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="container">	
	<!-- spacer image to make up for Bootstrap missing -->
	<img src="W-4.jpg" width="75" height="75" border = "none">

	<!-- navigation bar -->
	<div class="navbar navbar-fixed-top">
	 <div class="navbar-inner">
	   <div class="container">
	     <%= link_to "Wikiful", root_path, :class => "brand" %>
		<div class='session-controls'> 
		<% if current_user %>
		  <%= link_to "Log Out", logout_path, :class => 'btn btn-primary btn-mini' %>
		  Logged in as <%= current_user.email %>.
		<% else %>
		  <div class="btn-group">
		    <%= link_to "Sign Up", signup_path, :class => "btn btn-mini" %>
		    <%= link_to "Log In", login_path, :class => "btn btn-mini" %>
		  </div>
		<% end %>
		</div>
	   </div>
	 </div>
	</div>
	<div class="row">
	 <div class="span4">
	   <% flash.each do |name, msg| %>
	     <%= content_tag :div, msg, id: "flash_#{name}" %>
	   <% end %>
	 </div>
	</div>
	  <div class="row">
		  <div class="col-md-9"><%= yield %></div> 
	  
		  <div class="col-md-3 sidebar">
			  	 <div class="my-wiki-site">
	  	 	        <h4>My Wikiful</h4>
	  	 	     </div>
	  	<ul>
          <li><%= link_to "Write a Wiki", new_article_path %></li>
        </ul>
	  	  </div>
	  	  <div class="categories-sidebar">
	  	  </div>
	  	  <h4>Categories</h4>
	  	   <ul>
              <% @categories.each do |category| %>
                <li><%= link_to category.name, category %></li>
              <% end %>



	  </div>
</div>
</body>
</html>
